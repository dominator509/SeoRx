{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const file of ['package-lock.json','yarn.lock']) fs.rmSync(file,{force:true}); if (process.env.npm_config_user_agent && !process.env.npm_config_user_agent.includes('pnpm') && !process.env.npm_config_user_agent.includes('corepack')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "test": "corepack pnpm -r --if-present run test",
    "test:e2e:install": "corepack pnpm -r --if-present run test:e2e:install",
    "test:e2e": "corepack pnpm -r --if-present run test:e2e",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
